/**
 * Ícones — usamos o set do [Lucide](https://lucide.dev/icons/), padrão do shadcn/ui.
 * Este módulo apenas re-exporta (com apelidos) os ícones usados pela biblioteca,
 * servindo como catálogo do que está em uso. Para qualquer outro ícone, importe
 * direto de `lucide-react`:
 *
 *   import { Rocket } from "lucide-react";
 *
 * Todos aceitam `size`, `className`, `color`, `strokeWidth` e usam `currentColor`.
 */
export {
  MapPin as MapPinIcon,
  ChevronDown as ChevronDownIcon,
  ChevronLeft as ChevronLeftIcon,
  ChevronRight as ChevronRightIcon,
  ArrowRight as ArrowRightIcon,
  Plus as PlusIcon,
  Minus as MinusIcon,
  Check as CheckIcon,
  Search as SearchIcon,
  Building2 as BuildingIcon,
  Briefcase as BriefcaseIcon,
  Wallet as WalletIcon,
  FileText as FileTextIcon,
  GraduationCap as GraduationCapIcon,
  Users as UsersIcon,
  ShieldCheck as ShieldCheckIcon,
  Sparkles as SparklesIcon,
  Clock as ClockIcon,
} from "lucide-react";
